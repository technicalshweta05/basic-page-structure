# basic-page-structure
5-page basic site using HTML/CSS: structure with &lt;div>, &lt;a>, &lt;img>, semantic tags. CSS flex, box-sizing, margin, padding for layout. Styling includes fonts, spacing, transforms. Fixed-width layout with nav links across pages.
